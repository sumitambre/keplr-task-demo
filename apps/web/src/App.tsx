import { AppRouter } from './routes/Router';

function App() {
  // The App component is now very simple.
  // It just renders the router, which handles all the logic.
  return <AppRouter />;
}

export default App;